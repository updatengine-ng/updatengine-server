# Database settings
DB_NAME=updatengine
DB_USER=updatengineuser
DB_PASSWORD=updatenginepwd
DB_ROOTPASSWORD=rootpwd
DB_HOST=db
DB_PORT=3306

# URL used by client to connect to Updatengine-Server
SERVER_NAME=localhost

# Updatengine nginx port
PORT=1979

# Configuration type
# Valid values : empty, HTTP_REDIRECT or LETSENCRYPT
# LETSENCRYPT option required also to define
CONFIG_TYPE=

# Language for web interface. default to fr (French)
LANGUAGE_CODE=fr

# Debug mode
DEBUG=False

# Secret key
SECRET_KEY=!mustbechanged!

# Timezone
TZ='Europe/Paris'

# Email
ADMINS=('admin','admin@mail.tld'),
DEFAULT_FROM_EMAIL='updatengine@mail.tld'
SERVER_EMAIL='updatengine_error@mail.tld'
EMAIL_HOST=''
EMAIL_PORT=587
EMAIL_HOST_USER=''
EMAIL_HOST_PASSWORD=''
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
